// app/components/Logo.jsx
export function TextLogo({ className = "" }) {
  return (
   
      <div className={`font-bold ${className} tracking-tight text-red-600 flex items-center`}>
        7Meals.

    </div>
  );
}